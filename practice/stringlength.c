#include <stdio.h>
int main()
{
    char str[100];
    int i;
    printf("Enter the string: ");
    scanf("%s", str);
    for (i = 0; str[i] != '\0'; ++i)
    {
    }
    printf("The length of the given string is: %d\n", i);
    return (0);
}